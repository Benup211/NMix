import { defaultStyles } from "@/styles";
import { View, Text, StyleSheet } from "react-native";

export default function FavoritesPage() {
    return (
        <View style={defaultStyles.container}>
            <Text style={defaultStyles.text}>Favorites Page</Text>
        </View>
    );
}

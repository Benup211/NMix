import { defaultStyles } from "@/styles";
import { View, Text, StyleSheet } from "react-native";

export default function PlaylistsPage() {
    return (
        <View style={defaultStyles.container}>
            <Text style={defaultStyles.text}>Playlists Page</Text>
        </View>
    );
}
